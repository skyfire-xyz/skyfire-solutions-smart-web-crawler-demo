# Skip ci and use install instead
ci=false

# Skip audit during install
audit=false

# Skip funding messages
fund=false

# Use npm install instead of npm ci
prefer-install=install

# Skip optional dependencies
omit=optional

# Use exact versions (like npm ci but with install)
save-exact=true 